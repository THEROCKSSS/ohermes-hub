# Issue tracker for this project

**Platform: Forgejo** (confirmed by Owen 2026-07-30, when asking for
`IMPLEMENTATION_ROADMAP.md`'s phases to be filed as live-trackable issues).

- Repo: `claude-code/ohermes-hub` on the local Forgejo instance
  (`http://localhost:3000`) — the `claude-code` identity's own mirror, not
  the `agentsoul`-owned repo, per the forgejo-identity-separation rule.
- Issues #2–#6 are the open roadmap phases from `IMPLEMENTATION_ROADMAP.md`
  (Phase 0 is #1, closed — it was already shipped before filing).
- Label `roadmap` (id 187) marks issues sourced from that doc.
- GitHub (`THEROCKSSS/ohermes-hub`) remains the canonical public source
  repo; Forgejo is the issue tracker + private mirror, not the other way
  around.

Future engineering-flow work on this project (`/to-tickets`, `/triage`,
`/implement`, etc.) should keep writing issues/comments to this Forgejo
repo only — no re-asking, no also-posting to GitHub issues, per the
tracker-write rule in the global CLAUDE.md.
